[tool.poetry]
name = "yt2a-pr"
version = "0.1.0"
description = ""
authors = ["roman <roman.nieronov@mail.ru>"]
readme = "README.md"
packages = [{include = "yt2a_pr"}]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

